[
  {
    "name": "/get with no arguments shows usage",
    "steps": [
      {
        "send": { "text": "/get" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "No saved snippets. Use /save <name> <text> to create one."
            }
          }
        ]
      }
    ]
  }
]
